
public class GUIJFrame {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		
		// JFrame = a GUI window to add components to
		
		// JFrame frame = new JFrame(); // creates a frame
		
		// frame.setTitle("JFrame Title"); // set title of frame
		// frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // exit out of application
		// frame.setResizable(false); // prevent frame to resized
		// frame.setSize(420,420); // set the x-dim and y-dim
		// frame.setVisible(true); // show the frame
		
		// ImageIcon image = new ImageIcon("logo.png");
		// frame.setIconImage(image.getImage()); // change icon of frame
		// frame.getContentPane().setBackground(new Color(0,0,0)); // change background in (R,G,B)
		
		MyFrame myFrame = new MyFrame();
		
	}

}
